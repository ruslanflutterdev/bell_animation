# 🔔 Bell Animation App

**Bell Animation** — это демонстрационное мобильное приложение на Flutter, показывающее реалистичную анимацию "звонящего" колокольчика. Проект служит отличным примером работы с явными анимациями (Explicit Animations) и контроллерами во Flutter.

## ✨ Основные возможности

* **🔔 Анимация звонка**: При нажатии на колокольчик он начинает реалистично раскачиваться (звенеть).
* **🔄 Зацикливание**: Анимация может быть настроена на повторение (в коде используется `repeat(reverse: true)` или одиночный запуск).
* **👆 Интерактивность**: Запуск анимации происходит по клику пользователя (GestureDetector / InkWell).
* **🎨 Material Design**: Использование стандартных иконок и виджетов Material.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Анимация**:
    * `AnimationController` — управление временем и состоянием анимации.
    * `CurvedAnimation` — для придания естественности движению (кривая `Curves.easeInOut`).
    * `RotationTransition` — поворот виджета вокруг своей оси.
* **Архитектура**:
    * `StatefulWidget` — для хранения состояния контроллера анимации.
    * `SingleTickerProviderStateMixin` — для оптимизации кадров (VSync).

## 📂 Структура проекта

Проект компактен и понятен:

```text
lib/
├── screens/
│   └── bell_animation_screen.dart # Основной экран с логикой анимации
└── main.dart                      # Точка входа в приложение
